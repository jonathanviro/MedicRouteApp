<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.administrador.MedicosActivosActivity">

    <include
        android:id="@+id/toolbarMedicosActivos"
        layout="@layout/action_bar_toolbar" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMedicosActivos"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbarMedicosActivos"
        tools:listitem="@layout/item_medico" />
</androidx.constraintlayout.widget.ConstraintLayout>